From f4c1bb075b9f833fae8b693cda39a1625a3de191 Mon Sep 17 00:00:00 2001
From: Sam Shih <sam.shih@mediatek.com>
Date: Thu, 21 May 2026 17:23:07 +0800
Subject: [PATCH] update 2 ppe patches to new version

- 999-ppe-01-xt_FLOWOFFLOAD-add-bridging-support.patch
- 999-ppe-15-nft_flow_offload-add-bridging-support.patch

link:
https://git01.mediatek.com/plugins/gitiles/openwrt/feeds/mtk-openwrt-feeds/+/e55e9e94ea559b274620a9ceb2357c2d20c0902e
https://git01.mediatek.com/plugins/gitiles/openwrt/feeds/mtk-openwrt-feeds/+/db891987bb75c9b396d9d00730aa007230c92561
---
 ...-xt_FLOWOFFLOAD-add-bridging-support.patch | 117 +++++++--------
 ...ft_flow_offload-add-bridging-support.patch | 140 ++++++++----------
 2 files changed, 118 insertions(+), 139 deletions(-)

diff --git a/target/linux/mediatek/patches-6.12/999-ppe-01-xt_FLOWOFFLOAD-add-bridging-support.patch b/target/linux/mediatek/patches-6.12/999-ppe-01-xt_FLOWOFFLOAD-add-bridging-support.patch
index 1e11ccb78f..4de90b852e 100644
--- a/target/linux/mediatek/patches-6.12/999-ppe-01-xt_FLOWOFFLOAD-add-bridging-support.patch
+++ b/target/linux/mediatek/patches-6.12/999-ppe-01-xt_FLOWOFFLOAD-add-bridging-support.patch
@@ -1,9 +1,10 @@
 ---
-[v1] netfilter: add bridging support to xt_FLOWOFFLOAD
+[v2] netfilter: add bridging support to xt_FLOWOFFLOAD
 
 Without this patch, we are unable to use xt_FLOWOFFLOAD to perform
 bridging offload.
 ---
+v2: Fix IPv6 bridge offload fail without bridge master route
 v1: add bridging support to xt_FLOWOFFLOAD.
 ---
 
@@ -25,7 +26,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
 
 --- a/include/linux/netdevice.h
 +++ b/include/linux/netdevice.h
-@@ -3090,6 +3090,8 @@ int dev_get_iflink(const struct net_devi
+@@ -3092,6 +3092,8 @@ int dev_get_iflink(const struct net_devi
  int dev_fill_metadata_dst(struct net_device *dev, struct sk_buff *skb);
  int dev_fill_forward_path(const struct net_device *dev, const u8 *daddr,
  			  struct net_device_path_stack *stack);
@@ -321,7 +322,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  {
  	struct dst_entry *this_dst = skb_dst(skb);
  	struct dst_entry *other_dst = NULL;
-@@ -456,15 +543,86 @@ xt_flowoffload_route(struct sk_buff *skb
+@@ -456,10 +543,78 @@ xt_flowoffload_route(struct sk_buff *skb
  	nf_default_forward_path(route, this_dst, dir, devs);
  	nf_default_forward_path(route, other_dst, !dir, devs);
  
@@ -334,39 +335,8 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
 +			return -ENOENT;
 +		if (nf_dev_forward_path(skb, route, ct, !dir, devs))
 +			return -ENOENT;
- 	}
- 
- 	return 0;
- }
- 
-+static int
-+xt_flowoffload_route_dir(struct sk_buff *skb, struct nf_flow_route *route,
-+			 const struct nf_conn *ct, enum ip_conntrack_dir dir,
-+			 const struct xt_action_param *par, int ifindex,
-+			 struct net_device **devs)
-+{
-+	struct dst_entry *dst = NULL;
-+	struct flowi fl;
-+
-+	memset(&fl, 0, sizeof(fl));
-+	switch (xt_family(par)) {
-+	case NFPROTO_IPV4:
-+		fl.u.ip4.daddr = ct->tuplehash[!dir].tuple.src.u3.ip;
-+		fl.u.ip4.flowi4_oif = ifindex;
-+		break;
-+	case NFPROTO_IPV6:
-+		fl.u.ip6.saddr = ct->tuplehash[!dir].tuple.dst.u3.in6;
-+		fl.u.ip6.daddr = ct->tuplehash[!dir].tuple.src.u3.in6;
-+		fl.u.ip6.flowi6_oif = ifindex;
-+		break;
 +	}
 +
-+	nf_route(xt_net(par), &dst, &fl, false, xt_family(par));
-+	if (!dst)
-+		return -ENOENT;
-+
-+	nf_default_forward_path(route, dst, dir, devs);
-+
 +	return 0;
 +}
 +
@@ -376,42 +346,65 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
 +			      struct nf_flow_route *route, enum ip_conntrack_dir dir,
 +			      struct net_device **devs)
 +{
-+	int ret;
++	struct dst_entry *dsts[IP_CT_DIR_MAX] = {};
++	struct net_bridge_port *port;
++	struct net_device *br_dev;
++	int i;
 +
-+	ret = xt_flowoffload_route_dir(skb, route, ct, dir, par,
-+				       devs[dir]->ifindex,
-+				       devs);
-+	if (ret)
-+		return ret;
++	/* Allocate minimal dsts anchored to the bridge master device to
++	 * supply xmit_type and MTU for bridge forwarding.
++	 */
++	rcu_read_lock();
++	port = br_port_get_rcu(skb->dev);
++	if (!port) {
++		rcu_read_unlock();
++		return -ENOENT;
++	}
++	br_dev = port->br->dev;
 +
-+	ret = xt_flowoffload_route_dir(skb, route, ct, !dir, par,
-+				       devs[!dir]->ifindex,
-+				       devs);
-+	if (ret)
-+		goto err_route_dir1;
++	for (i = 0; i < IP_CT_DIR_MAX; i++) {
++		switch (xt_family(par)) {
++		case NFPROTO_IPV4: {
++			struct rtable *rt;
 +
-+	if (route->tuple[dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH &&
-+	    route->tuple[!dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH) {
-+		if (nf_dev_forward_path(skb, route, ct, dir, devs) ||
-+		    nf_dev_forward_path(skb, route, ct, !dir, devs)) {
-+			ret = -ENOENT;
-+			goto err_route_dir2;
++			rt = rt_dst_alloc(br_dev, 0, RTN_UNICAST, true);
++			if (rt)
++				dsts[i] = &rt->dst;
++			break;
++		}
++		case NFPROTO_IPV6: {
++			struct rt6_info *rt;
++
++			rt = ip6_dst_alloc(xt_net(par), br_dev, 0);
++			if (rt)
++				dsts[i] = &rt->dst;
++			break;
++		}
 +		}
 +	}
++	rcu_read_unlock();
 +
-+	return 0;
++	if (!dsts[dir] || !dsts[!dir]) {
++		dst_release(dsts[dir]);
++		dst_release(dsts[!dir]);
++		return -ENOENT;
++	}
 +
-+err_route_dir2:
-+	dst_release(route->tuple[!dir].dst);
-+err_route_dir1:
-+	dst_release(route->tuple[dir].dst);
-+	return ret;
-+}
++	nf_default_forward_path(route, dsts[dir], dir, devs);
++	nf_default_forward_path(route, dsts[!dir], !dir, devs);
 +
- static unsigned int
- flowoffload_tg(struct sk_buff *skb, const struct xt_action_param *par)
- {
-@@ -520,8 +678,13 @@ flowoffload_tg(struct sk_buff *skb, cons
++	if (route->tuple[dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH &&
++	    route->tuple[!dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH) {
++		if (nf_dev_forward_path(skb, route, ct, dir, devs) ||
++		    nf_dev_forward_path(skb, route, ct, !dir, devs)) {
++			dst_release(route->tuple[dir].dst);
++			dst_release(route->tuple[!dir].dst);
++			return -ENOENT;
++		}
+ 	}
+ 
+ 	return 0;
+@@ -520,8 +675,13 @@ flowoffload_tg(struct sk_buff *skb, cons
  	if (test_and_set_bit(IPS_OFFLOAD_BIT, &ct->status))
  		return XT_CONTINUE;
  
diff --git a/target/linux/mediatek/patches-6.12/999-ppe-15-nft_flow_offload-add-bridging-support.patch b/target/linux/mediatek/patches-6.12/999-ppe-15-nft_flow_offload-add-bridging-support.patch
index 08bd27e205..6ebfc270d8 100644
--- a/target/linux/mediatek/patches-6.12/999-ppe-15-nft_flow_offload-add-bridging-support.patch
+++ b/target/linux/mediatek/patches-6.12/999-ppe-15-nft_flow_offload-add-bridging-support.patch
@@ -1,9 +1,11 @@
 ---
-[v1] netfilter: add bridging support to the nft_flow_offload
+[v3] netfilter: add bridging support to the nft_flow_offload
 
 Without this patch, the bridging traffic is unable to BIND using
 nft_flow_offload.
 ---
+v3: Fix bridge VLAN untag underflow WARN.
+v2: Fix IPv6 bridge offload fail without bridge master route.
 v1: Add bridging support to nft_flow_offload.
 ---
 
@@ -131,23 +133,20 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  			if (is_zero_ether_addr(info->h_source))
  				memcpy(info->h_source, path->dev->dev_addr, ETH_ALEN);
  
-@@ -150,9 +213,12 @@ static void nft_dev_path_info(const stru
+@@ -150,10 +213,8 @@ static void nft_dev_path_info(const stru
  				info->num_encaps++;
  				break;
  			case DEV_PATH_BR_VLAN_UNTAG:
 -				if (WARN_ON_ONCE(info->num_encaps-- == 0)) {
 -					info->indev = NULL;
 -					break;
-+				if (info->num_encaps > 0) {
+-				}
++				if (info->num_encaps > 0)
 +					info->num_encaps--;
-+					if (WARN_ON_ONCE(info->num_encaps == 0)) {
-+						info->indev = NULL;
-+ 						break;
-+					}
- 				}
  				break;
  			case DEV_PATH_BR_VLAN_KEEP:
-@@ -161,7 +227,6 @@ static void nft_dev_path_info(const stru
+ 				break;
+@@ -161,7 +222,6 @@ static void nft_dev_path_info(const stru
  			info->xmit_type = FLOW_OFFLOAD_XMIT_DIRECT;
  			break;
  		default:
@@ -155,7 +154,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  			break;
  		}
  	}
-@@ -192,22 +257,44 @@ static bool nft_flowtable_find_dev(const
+@@ -192,22 +252,44 @@ static bool nft_flowtable_find_dev(const
  	return found;
  }
  
@@ -206,7 +205,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  
  	route->tuple[!dir].in.ifindex = info.indev->ifindex;
  	for (i = 0; i < info.num_encaps; i++) {
-@@ -224,13 +311,15 @@ static void nft_dev_forward_path(struct
+@@ -224,13 +306,15 @@ static void nft_dev_forward_path(struct
  		route->tuple[dir].out.hw_ifindex = info.hw_outdev->ifindex;
  		route->tuple[dir].xmit_type = info.xmit_type;
  	}
@@ -227,7 +226,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  {
  	struct dst_entry *this_dst = skb_dst(pkt->skb);
  	struct dst_entry *other_dst = NULL;
-@@ -272,13 +361,89 @@ static int nft_flow_route(const struct n
+@@ -272,8 +356,76 @@ static int nft_flow_route(const struct n
  
  	if (route->tuple[dir].xmit_type	== FLOW_OFFLOAD_XMIT_NEIGH &&
  	    route->tuple[!dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH) {
@@ -236,46 +235,8 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
 +		if (nft_dev_forward_path(pkt, route, ct, dir, ft) ||
 +		    nft_dev_forward_path(pkt, route, ct, !dir, ft))
 +		    return -ENOENT;
- 	}
- 
- 	return 0;
- }
- 
-+static int
-+nft_flow_route_dir(const struct nft_pktinfo *pkt,
-+		   const struct nf_conn *ct,
-+		   struct nf_flow_route *route,
-+		   enum ip_conntrack_dir dir,
-+		   int ifindex)
-+{
-+	struct dst_entry *other_dst = NULL;
-+	struct flowi fl;
-+
-+	memset(&fl, 0, sizeof(fl));
-+	switch (nft_pf(pkt)) {
-+	case NFPROTO_IPV4:
-+		fl.u.ip4.daddr = ct->tuplehash[!dir].tuple.src.u3.ip;
-+		fl.u.ip4.flowi4_oif = ifindex;
-+		fl.u.ip4.flowi4_tos = RT_TOS(ip_hdr(pkt->skb)->tos);
-+		fl.u.ip4.flowi4_mark = pkt->skb->mark;
-+		fl.u.ip4.flowi4_flags = FLOWI_FLAG_ANYSRC;
-+		break;
-+	case NFPROTO_IPV6:
-+		fl.u.ip6.saddr = ct->tuplehash[!dir].tuple.dst.u3.in6;
-+		fl.u.ip6.daddr = ct->tuplehash[!dir].tuple.src.u3.in6;
-+		fl.u.ip6.flowi6_oif = ifindex;
-+		fl.u.ip6.flowlabel = ip6_flowinfo(ipv6_hdr(pkt->skb));
-+		fl.u.ip6.flowi6_mark = pkt->skb->mark;
-+		fl.u.ip6.flowi6_flags = FLOWI_FLAG_ANYSRC;
-+		break;
 +	}
 +
-+	nf_route(nft_net(pkt), &other_dst, &fl, false, nft_pf(pkt));
-+	if (!other_dst)
-+		return -ENOENT;
-+
-+	nft_default_forward_path(route, other_dst, dir);
-+
 +	return 0;
 +}
 +
@@ -286,40 +247,65 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
 +			enum ip_conntrack_dir dir,
 +			struct nft_flowtable *ft)
 +{
-+	int ret;
++	struct dst_entry *dsts[IP_CT_DIR_MAX] = {};
++	struct net_bridge_port *port;
++	struct net_device *br_dev;
++	int i;
 +
-+	ret = nft_flow_route_dir(pkt, ct, route, dir,
-+				 nft_out(pkt)->ifindex);
-+	if (ret)
-+		return ret;
++	/* Allocate minimal dsts anchored to the bridge master device to
++	 * supply xmit_type and MTU for bridge forwarding.
++	 */
++	rcu_read_lock();
++	port = br_port_get_rcu(pkt->skb->dev);
++	if (!port) {
++		rcu_read_unlock();
++		return -ENOENT;
++	}
++	br_dev = port->br->dev;
 +
-+	ret = nft_flow_route_dir(pkt, ct, route, !dir,
-+				 nft_in(pkt)->ifindex);
-+	if (ret)
-+		goto err_route_dir1;
++	for (i = 0; i < IP_CT_DIR_MAX; i++) {
++		switch (nft_pf(pkt)) {
++		case NFPROTO_IPV4: {
++			struct rtable *rt;
 +
-+	if (route->tuple[dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH &&
-+	    route->tuple[!dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH) {
-+		if (nft_dev_forward_path(pkt, route, ct, dir, ft) ||
-+		    nft_dev_forward_path(pkt, route, ct, !dir, ft)) {
-+			ret = -ENOENT;
-+			goto err_route_dir2;
++			rt = rt_dst_alloc(br_dev, 0, RTN_UNICAST, true);
++			if (rt)
++				dsts[i] = &rt->dst;
++			break;
++		}
++		case NFPROTO_IPV6: {
++			struct rt6_info *rt;
++
++			rt = ip6_dst_alloc(nft_net(pkt), br_dev, 0);
++			if (rt)
++				dsts[i] = &rt->dst;
++			break;
++		}
 +		}
 +	}
++	rcu_read_unlock();
 +
-+	return 0;
++	if (!dsts[dir] || !dsts[!dir]) {
++		dst_release(dsts[dir]);
++		dst_release(dsts[!dir]);
++		return -ENOENT;
++	}
 +
-+err_route_dir2:
-+	dst_release(route->tuple[!dir].dst);
-+err_route_dir1:
-+	dst_release(route->tuple[dir].dst);
-+	return ret;
-+}
++	nft_default_forward_path(route, dsts[dir], dir);
++	nft_default_forward_path(route, dsts[!dir], !dir);
 +
- static bool nft_flow_offload_skip(struct sk_buff *skb, int family)
- {
- 	if (skb_sec_path(skb))
-@@ -328,10 +493,12 @@ static void nft_flow_offload_eval(const
++	if (route->tuple[dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH &&
++	    route->tuple[!dir].xmit_type == FLOW_OFFLOAD_XMIT_NEIGH) {
++		if (nft_dev_forward_path(pkt, route, ct, dir, ft) ||
++		    nft_dev_forward_path(pkt, route, ct, !dir, ft)) {
++			dst_release(route->tuple[dir].dst);
++			dst_release(route->tuple[!dir].dst);
++			return -ENOENT;
++		}
+ 	}
+ 
+ 	return 0;
+@@ -328,10 +480,12 @@ static void nft_flow_offload_eval(const
  
  	switch (ct->tuplehash[IP_CT_DIR_ORIGINAL].tuple.dst.protonum) {
  	case IPPROTO_TCP:
@@ -334,7 +320,7 @@ Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
  			goto out;
  		break;
  	case IPPROTO_UDP:
-@@ -364,8 +531,13 @@ static void nft_flow_offload_eval(const
+@@ -364,8 +518,13 @@ static void nft_flow_offload_eval(const
  		goto out;
  
  	dir = CTINFO2DIR(ctinfo);
-- 
2.45.2

