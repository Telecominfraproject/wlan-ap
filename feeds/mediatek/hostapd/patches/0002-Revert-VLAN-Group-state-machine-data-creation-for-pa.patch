From 387fadc9d25f1b06c74556c99162c7354c040c79 Mon Sep 17 00:00:00 2001
From: Shayne Chen <shayne.chen@mediatek.com>
Date: Thu, 26 Feb 2026 12:09:41 +0800
Subject: [PATCH 002/260] Revert "VLAN: Group state machine data creation for
 partner AP MLD links"

This reverts commit db4908d1c5d6bde34636f95176309db7aab7db5c.
---
 src/ap/sta_info.c | 75 +++--------------------------------------------
 src/ap/wpa_auth.c | 39 +++++-------------------
 src/ap/wpa_auth.h |  3 +-
 3 files changed, 12 insertions(+), 105 deletions(-)

diff --git a/src/ap/sta_info.c b/src/ap/sta_info.c
index fc16b1a42..ec91cce89 100644
--- a/src/ap/sta_info.c
+++ b/src/ap/sta_info.c
@@ -1299,9 +1299,8 @@ retry:
 }
 
 
-static int ap_sta_set_vlan_helper(struct hostapd_data *hapd,
-				  struct sta_info *sta,
-				  struct vlan_description *vlan_desc)
+int ap_sta_set_vlan(struct hostapd_data *hapd, struct sta_info *sta,
+		    struct vlan_description *vlan_desc)
 {
 	struct hostapd_vlan *vlan = NULL, *wildcard_vlan = NULL;
 	struct hostapd_data *vlan_bss = hapd;
@@ -1412,41 +1411,7 @@ done:
 }
 
 
-int ap_sta_set_vlan(struct hostapd_data *hapd, struct sta_info *sta,
-		    struct vlan_description *vlan_desc)
-{
-	int ret;
-#ifdef CONFIG_IEEE80211BE
-	size_t i;
-	struct hapd_interfaces *interfaces = hapd->iface->interfaces;
-#endif /* CONFIG_IEEE80211BE */
-
-	ret = ap_sta_set_vlan_helper(hapd, sta, vlan_desc);
-	if (ret)
-		return ret;
-#ifdef CONFIG_IEEE80211BE
-	for (i = 0; interfaces && i < interfaces->count; i++) {
-		struct sta_info *tmp_sta;
-		struct hostapd_data *tmp_hapd = interfaces->iface[i]->bss[0];
-
-		if (!tmp_hapd->conf->mld_ap ||
-		    hapd == tmp_hapd ||
-		    !hostapd_is_ml_partner(hapd, tmp_hapd))
-			continue;
-
-		tmp_sta = ap_get_sta(tmp_hapd, sta->addr);
-		if (tmp_sta)
-			ap_sta_set_vlan_helper(tmp_hapd, tmp_sta, vlan_desc);
-	}
-
-#endif /* CONFIG_IEEE80211BE */
-
-	return 0;
-}
-
-
-static int ap_sta_bind_vlan_helper(struct hostapd_data *hapd,
-				   struct sta_info *sta)
+int ap_sta_bind_vlan(struct hostapd_data *hapd, struct sta_info *sta)
 {
 #ifndef CONFIG_NO_VLAN
 	const char *iface;
@@ -1518,8 +1483,7 @@ skip_counting:
 		       HOSTAPD_LEVEL_DEBUG, "binding station to interface "
 		       "'%s'", iface);
 
-	if (wpa_auth_sta_set_vlan(sta->wpa_sm, hapd->wpa_auth,
-				  sta->vlan_id) < 0)
+	if (wpa_auth_sta_set_vlan(sta->wpa_sm, sta->vlan_id) < 0)
 		wpa_printf(MSG_INFO, "Failed to update VLAN-ID for WPA");
 
 	ret = hostapd_drv_set_sta_vlan(iface, hapd, sta->addr, sta->vlan_id,
@@ -1542,37 +1506,6 @@ done:
 }
 
 
-int ap_sta_bind_vlan(struct hostapd_data *hapd, struct sta_info *sta)
-{
-	int ret;
-#ifdef CONFIG_IEEE80211BE
-	size_t i;
-	struct hapd_interfaces *interfaces = hapd->iface->interfaces;
-#endif /* CONFIG_IEEE80211BE */
-
-	ret = ap_sta_bind_vlan_helper(hapd, sta);
-	if (ret)
-		return ret;
-#ifdef CONFIG_IEEE80211BE
-	for (i = 0; interfaces && i < interfaces->count; i++) {
-		struct sta_info *tmp_sta;
-		struct hostapd_data *tmp_hapd = interfaces->iface[i]->bss[0];
-
-		if (!tmp_hapd->conf->mld_ap ||
-		    hapd == tmp_hapd ||
-		    !hostapd_is_ml_partner(hapd, tmp_hapd))
-			continue;
-
-		tmp_sta = ap_get_sta(tmp_hapd, sta->addr);
-		if (tmp_sta)
-			ap_sta_bind_vlan_helper(tmp_hapd, tmp_sta);
-	}
-#endif /* CONFIG_IEEE80211BE */
-
-	return 0;
-}
-
-
 void ap_sta_set_sa_query_timeout(struct hostapd_data *hapd,
 				 struct sta_info *sta, int value)
 {
diff --git a/src/ap/wpa_auth.c b/src/ap/wpa_auth.c
index d1ed44d7e..857dd332f 100644
--- a/src/ap/wpa_auth.c
+++ b/src/ap/wpa_auth.c
@@ -70,10 +70,6 @@ static void wpa_group_get(struct wpa_authenticator *wpa_auth,
 			  struct wpa_group *group);
 static void wpa_group_put(struct wpa_authenticator *wpa_auth,
 			  struct wpa_group *group);
-#ifdef CONFIG_IEEE80211BE
-static void wpa_group_put_vlan(struct wpa_authenticator *wpa_auth,
-			       int vlan_id);
-#endif /* CONFIG_IEEE80211BE */
 static int ieee80211w_kde_len(struct wpa_state_machine *sm);
 static u8 * ieee80211w_kde_add(struct wpa_state_machine *sm, u8 *pos);
 static int wpa_group_update_gtk(struct wpa_authenticator *wpa_auth,
@@ -151,7 +147,7 @@ void wpa_release_link_auth_ref(struct wpa_state_machine *sm, u8 link_id,
 		wpa_auth = link->wpa_auth;
 		if (wpa_auth) {
 			link->wpa_auth = NULL;
-			wpa_group_put_vlan(wpa_auth, sm->group->vlan_id);
+			wpa_group_put(wpa_auth, wpa_auth->group);
 		}
 	}
 }
@@ -1171,7 +1167,7 @@ static void wpa_free_sta_sm(struct wpa_state_machine *sm)
 		wpa_auth = sm->mld_links[link_id].wpa_auth;
 		sm->mld_links[link_id].wpa_auth = NULL;
 		sm->mld_links[link_id].valid = false;
-		wpa_group_put_vlan(wpa_auth, sm->group->vlan_id);
+		wpa_group_put(wpa_auth, wpa_auth->group);
 	}
 #endif /* CONFIG_IEEE80211BE */
 	wpa_group_put(sm->wpa_auth, sm->group);
@@ -4388,8 +4384,8 @@ void wpa_auth_ml_get_key_info(struct wpa_authenticator *a,
 	u8 rsc[WPA_KEY_RSC_LEN];
 
 	wpa_printf(MSG_DEBUG,
-		   "MLD: Get group key info: link_id=%u, IGTK=%u, BIGTK=%u VLAN ID:%d",
-		   info->link_id, mgmt_frame_prot, beacon_prot, vlan_id);
+		   "MLD: Get group key info: link_id=%u, IGTK=%u, BIGTK=%u",
+		   info->link_id, mgmt_frame_prot, beacon_prot);
 
 	if (vlan_id)
 		gsm = wpa_select_vlan_wpa_group(gsm, vlan_id);
@@ -7102,18 +7098,6 @@ static void wpa_group_put(struct wpa_authenticator *wpa_auth,
 }
 
 
-#ifdef CONFIG_IEEE80211BE
-static void wpa_group_put_vlan(struct wpa_authenticator *wpa_auth,
-			       int vlan_id)
-{
-	struct wpa_group *vlan_group =
-		wpa_select_vlan_wpa_group(wpa_auth->group, vlan_id);
-
-	wpa_group_put(wpa_auth, vlan_group);
-}
-#endif /* CONFIG_IEEE80211BE */
-
-
 /*
  * Add a group that has its references counter set to zero. Caller needs to
  * call wpa_group_get() on the return value to mark the entry in use.
@@ -7231,15 +7215,14 @@ int wpa_auth_release_group(struct wpa_authenticator *wpa_auth, int vlan_id)
 }
 
 
-int wpa_auth_sta_set_vlan(struct wpa_state_machine *sm,
-			  struct wpa_authenticator *wpa_auth, int vlan_id)
+int wpa_auth_sta_set_vlan(struct wpa_state_machine *sm, int vlan_id)
 {
 	struct wpa_group *group;
 
 	if (!sm || !sm->wpa_auth)
 		return 0;
 
-	group = wpa_auth->group;
+	group = sm->wpa_auth->group;
 	while (group) {
 		if (group->vlan_id == vlan_id)
 			break;
@@ -7247,19 +7230,11 @@ int wpa_auth_sta_set_vlan(struct wpa_state_machine *sm,
 	}
 
 	if (!group) {
-		group = wpa_auth_add_group(wpa_auth, vlan_id);
+		group = wpa_auth_add_group(sm->wpa_auth, vlan_id);
 		if (!group)
 			return -1;
 	}
 
-#ifdef CONFIG_IEEE80211BE
-	if (sm->mld_assoc_link_id >= 0 &&
-	    (sm->mld_assoc_link_id != wpa_auth->link_id)) {
-		wpa_group_get(wpa_auth, group);
-		return 0;
-	}
-#endif /* CONFIG_IEEE80211BE */
-
 	if (sm->group == group)
 		return 0;
 
diff --git a/src/ap/wpa_auth.h b/src/ap/wpa_auth.h
index 646c76a90..0f0a1ac7d 100644
--- a/src/ap/wpa_auth.h
+++ b/src/ap/wpa_auth.h
@@ -557,8 +557,7 @@ void wpa_auth_pmksa_set_to_sm(struct rsn_pmksa_cache_entry *pmksa,
 			      struct wpa_state_machine *sm,
 			      struct wpa_authenticator *wpa_auth,
 			      u8 *pmkid, u8 *pmk, size_t *pmk_len);
-int wpa_auth_sta_set_vlan(struct wpa_state_machine *sm,
-			  struct wpa_authenticator *wpa_auth, int vlan_id);
+int wpa_auth_sta_set_vlan(struct wpa_state_machine *sm, int vlan_id);
 void wpa_auth_eapol_key_tx_status(struct wpa_authenticator *wpa_auth,
 				  struct wpa_state_machine *sm, int ack);
 
-- 
2.45.2

