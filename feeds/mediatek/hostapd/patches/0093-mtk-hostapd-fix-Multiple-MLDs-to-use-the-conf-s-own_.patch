From 0f55511e4b1c9cb69f88366eb8dfa63e87dc79dd Mon Sep 17 00:00:00 2001
From: Bo Jiao <Bo.Jiao@mediatek.com>
Date: Wed, 10 Jul 2024 14:49:43 +0800
Subject: [PATCH 093/260] mtk: hostapd: fix Multiple MLDs to use the conf's
 own_addr/mld_addr.

Signed-off-by: Bo Jiao <Bo.Jiao@mediatek.com>
---
 src/ap/hostapd.c | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/src/ap/hostapd.c b/src/ap/hostapd.c
index f3ee6f7f0..643448542 100644
--- a/src/ap/hostapd.c
+++ b/src/ap/hostapd.c
@@ -1566,6 +1566,9 @@ int hostapd_setup_bss(struct hostapd_data *hapd, int first, bool start_beacon)
 				goto setup_mld;
 			}
 			use_existing = true;
+
+			if (addr && !is_zero_ether_addr(hapd->conf->mld_addr))
+				os_memcpy(addr, hapd->conf->mld_addr, ETH_ALEN);
 		}
 #endif /* CONFIG_IEEE80211BE */
 
@@ -1591,6 +1594,9 @@ int hostapd_setup_bss(struct hostapd_data *hapd, int first, bool start_beacon)
 				   hapd->mld_link_id, hapd->conf->iface);
 			os_memcpy(hapd->mld->mld_addr, hapd->own_addr,
 				  ETH_ALEN);
+
+			if (!is_zero_ether_addr(conf->bssid))
+				os_memcpy(hapd->own_addr, conf->bssid, ETH_ALEN);
 		}
 #endif /* CONFIG_IEEE80211BE */
 	}
-- 
2.45.2

