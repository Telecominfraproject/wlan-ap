--- a/src/cm2/src/cm2_event.c
+++ b/src/cm2/src/cm2_event.c
@@ -334,6 +334,7 @@ static void cm2_compute_backoff(void)
         goto set_backoff;
     }
     int ret = read(fd, &backoff, sizeof(backoff));
+    close(fd);
     if (ret <= 0) {
         LOGE("Error reading /dev/urandom");
         goto set_backoff;
