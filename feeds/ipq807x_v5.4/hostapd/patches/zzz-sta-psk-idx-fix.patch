--- a/src/ap/wpa_auth_glue.c
+++ b/src/ap/wpa_auth_glue.c
@@ -413,6 +413,7 @@ static const u8 * hostapd_wpa_auth_get_psk(void *ctx, const u8 *addr,
 					sta->psk_idx = psk_idx + 1;
 				break;
 			}
+			psk_idx++;
 		}
 		if (vlan_id && !psk)
 			sta->psk_idx = 0;
