From 97edb7ae1086187fac5290826ad19b0df140bd71 Mon Sep 17 00:00:00 2001
From: Hari Naraayana Desikan Kannan <hnaraaya@qti.qualcomm.com>
Date: Thu, 10 Jul 2025 10:10:40 +0530
Subject: [PATCH 1/3] UPSTREAM:PROTOCOL: wifi: ath12k: Refactor UMAC migration
 logic into internal helper

Move the logic for checking primary link removal and performing
UMAC migration into a new static helper function,
ath12k_primary_link_migrate().

This logic was previously implemented directly in the exported
function ath12k_mac_op_removed_link_is_primary(), which is
registered as the driver's removed_link_is_primary callback and
invoked by mac80211.

The helper performs both the primary link check and initiates
UMAC migration if required. This refactoring improves modularity
and no functional changes intended.

Signed-off-by: Hari Naraayana Desikan Kannan <hnaraaya@qti.qualcomm.com>
---
 drivers/net/wireless/ath/ath12k/mac.c | 10 ++++++++--
 1 file changed, 8 insertions(+), 2 deletions(-)

--- a/drivers/net/wireless/ath/ath12k/mac.c
+++ b/drivers/net/wireless/ath/ath12k/mac.c
@@ -11442,10 +11442,87 @@ void ath12k_mac_assign_middle_link_id(st
 			return;
 		}
 	}
 }
 
+static bool ath12k_is_primary_link_migrate(struct ieee80211_sta *sta,
+					   u16 removed_links)
+{
+	struct ath12k_mac_link_migrate_usr_params migrate_params = {0};
+	struct ath12k_sta *ahsta = ath12k_sta_to_ahsta(sta);
+	unsigned long removed_link = removed_links;
+	struct ath12k_vif *ahvif = ahsta->ahvif;
+	struct ath12k_hw *ah = ahvif->ah;
+	struct ath12k_link_vif *arvif;
+	bool is_primary = false;
+	struct ath12k *ar;
+	u16 link_id;
+	int ret;
+
+	mutex_lock(&ah->hw_mutex);
+
+	for_each_set_bit(link_id, &removed_link, ATH12K_NUM_MAX_LINKS) {
+		if (ahsta->primary_link_id == link_id) {
+			is_primary = true;
+			break;
+		}
+	}
+
+	/* if not primary, just return now */
+	if (!is_primary) {
+		mutex_unlock(&ah->hw_mutex);
+		return false;
+	}
+
+	/* if it happens to be primary link, trigger UMAC Migration. */
+	arvif = ath12k_get_arvif_from_link_id(ahvif, ahsta->primary_link_id);
+	if (WARN_ON(!arvif || !arvif->is_up || !arvif->ar)) {
+		mutex_unlock(&ah->hw_mutex);
+		return true;
+	}
+
+	ar = arvif->ar;
+
+	migrate_params.link_id = 0xFF;
+	memcpy(migrate_params.addr, sta->addr, ETH_ALEN);
+
+	arvif->is_link_removal_in_progress = true;
+
+	ret = ath12k_mac_process_link_migrate_req(ahvif, &migrate_params);
+	if (ret) {
+		arvif->is_link_removal_in_progress = false;
+		mutex_unlock(&ah->hw_mutex);
+		return true;
+	}
+
+	ath12k_dbg(ar->ab, ATH12K_DBG_MAC,
+		   "mac waiting for UMAC migration to finish\n");
+
+	/* Wait for migration to finish. If it timed out, just WARN_ON()
+	 * and continue since in this case, primary link will still match
+	 * and whole MLD will disconnect anyway
+	 */
+	if (!wait_for_completion_timeout(&arvif->wmi_migration_event_resp,
+					 ATH12K_MIGRATION_TIMEOUT_HZ))
+		WARN_ON(1);
+
+	arvif->is_link_removal_in_progress = false;
+
+	/* now check again and accordingly return */
+	is_primary = false;
+	for_each_set_bit(link_id, &removed_link, ATH12K_NUM_MAX_LINKS) {
+		if (ahsta->primary_link_id == link_id) {
+			is_primary = true;
+			break;
+		}
+	}
+
+	mutex_unlock(&ah->hw_mutex);
+
+	return is_primary;
+}
+
 int ath12k_mac_op_change_sta_links(struct ieee80211_hw *hw,
 				   struct ieee80211_vif *vif,
 				   struct ieee80211_sta *sta,
 				   u16 old_links, u16 new_links)
 {
@@ -12182,82 +12259,11 @@ exit_link_migrate_req:
 }
 
 bool ath12k_mac_op_removed_link_is_primary(struct ieee80211_sta *sta,
 					   u16 removed_links)
 {
-	struct ath12k_mac_link_migrate_usr_params migrate_params = {0};
-	struct ath12k_sta *ahsta = ath12k_sta_to_ahsta(sta);
-	unsigned long removed_link = removed_links;
-	struct ath12k_vif *ahvif = ahsta->ahvif;
-	struct ath12k_hw *ah = ahvif->ah;
-	struct ath12k_link_vif *arvif;
-	bool is_primary = false;
-	struct ath12k *ar;
-	u16 link_id;
-	int ret;
-
-	mutex_lock(&ah->hw_mutex);
-
-	for_each_set_bit(link_id, &removed_link, ATH12K_NUM_MAX_LINKS) {
-		if (ahsta->primary_link_id == link_id) {
-			is_primary = true;
-			break;
-		}
-	}
-
-	/* if not primary, just return now */
-	if (!is_primary) {
-		mutex_unlock(&ah->hw_mutex);
-		return false;
-	}
-
-	/* if it happens to be primary link, trigger UMAC Migration. */
-	arvif = ath12k_get_arvif_from_link_id(ahvif, ahsta->primary_link_id);
-	if (WARN_ON(!arvif || !arvif->is_up || !arvif->ar)) {
-		mutex_unlock(&ah->hw_mutex);
-		return true;
-	}
-
-	ar = arvif->ar;
-
-	migrate_params.link_id = 0xFF;
-	memcpy(migrate_params.addr, sta->addr, ETH_ALEN);
-
-	arvif->is_link_removal_in_progress = true;
-
-	ret = ath12k_mac_process_link_migrate_req(ahvif, &migrate_params);
-	if (ret) {
-		arvif->is_link_removal_in_progress = false;
-		mutex_unlock(&ah->hw_mutex);
-		return true;
-	}
-
-	ath12k_dbg(ar->ab, ATH12K_DBG_MAC,
-		   "mac waiting for UMAC migration to finish\n");
-
-	/* Wait for migration to finish. If it timed out, just WARN_ON()
-	 * and continue since in this case, primary link will still match
-	 * and whole MLD will disconnect anyway
-	 */
-	if (!wait_for_completion_timeout(&arvif->wmi_migration_event_resp,
-					 ATH12K_MIGRATION_TIMEOUT_HZ))
-		WARN_ON(1);
-
-	arvif->is_link_removal_in_progress = false;
-
-	/* now check again and accordingly return */
-	is_primary = false;
-	for_each_set_bit(link_id, &removed_link, ATH12K_NUM_MAX_LINKS) {
-		if (ahsta->primary_link_id == link_id) {
-			is_primary = true;
-			break;
-		}
-	}
-
-	mutex_unlock(&ah->hw_mutex);
-
-	return is_primary;
+	return ath12k_is_primary_link_migrate(sta, removed_links);
 }
 EXPORT_SYMBOL(ath12k_mac_op_removed_link_is_primary);
 
 bool ath12k_mac_op_can_activate_links(struct ieee80211_hw *hw,
 				      struct ieee80211_vif *vif,
