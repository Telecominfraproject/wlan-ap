Index: hostapd-2025.03.10~95ad7115/src/ap/ieee802_11.c
===================================================================
--- hostapd-2025.03.10~95ad7115.orig/src/ap/ieee802_11.c
+++ hostapd-2025.03.10~95ad7115/src/ap/ieee802_11.c
@@ -558,6 +558,7 @@ static void handle_auth_ft_finish(void *
 
 	hostapd_logger(hapd, dst, HOSTAPD_MODULE_IEEE80211,
 		       HOSTAPD_LEVEL_DEBUG, "authentication OK (FT)");
+	hostapd_ubus_notify(hapd, "ft-finish", sta->addr);
 	sta->flags |= WLAN_STA_AUTH;
 	mlme_authenticate_indication(hapd, sta);
 }
@@ -6371,6 +6372,9 @@ static void handle_assoc(struct hostapd_
 	}
 #endif /* CONFIG_IEEE80211BE */
 
+	if (sta && reassoc)
+		memcpy(sta->origin_ap, mgmt->u.reassoc_req.current_ap, 6);
+
 #ifdef CONFIG_IEEE80211R_AP
 	if (!sta) {
 		sta = ap_get_unadded_sta(hapd, mgmt->sa);
@@ -6557,6 +6561,7 @@ static void handle_assoc(struct hostapd_
 		.type = HOSTAPD_UBUS_ASSOC_REQ,
 		.mgmt_frame = mgmt,
 		.ssi_signal = rssi,
+		.reassoc = reassoc,
 	};
 
 	if ((sta->flags & WLAN_STA_MFP) &&
@@ -7886,7 +7891,7 @@ static void handle_assoc_cb(struct hosta
 		 * Open, static WEP, FT protocol, or FILS; no separate
 		 * authorization step.
 		 */
-		ap_sta_set_authorized(hapd, sta, 1);
+		_ap_sta_set_authorized(hapd, sta, 1, reassoc);
 	}
 
 	if (sta->auth_alg == WLAN_AUTH_FT) {
Index: hostapd-2025.03.10~95ad7115/src/ap/sta_info.c
===================================================================
--- hostapd-2025.03.10~95ad7115.orig/src/ap/sta_info.c
+++ hostapd-2025.03.10~95ad7115/src/ap/sta_info.c
@@ -2013,9 +2013,14 @@ bool ap_sta_set_authorized_flag(struct h
 	return true;
 }
 
+void ap_sta_set_authorized_event(struct hostapd_data *hapd, 
+				struct sta_info *sta, int authorized)
+{
+	__ap_sta_set_authorized_event(hapd, sta, authorized, 0);
+}
 
-void ap_sta_set_authorized_event(struct hostapd_data *hapd,
-				 struct sta_info *sta, int authorized)
+void __ap_sta_set_authorized_event(struct hostapd_data *hapd,
+				 struct sta_info *sta, int authorized, int reassoc)
 {
 	const u8 *dev_addr = NULL;
 	char buf[100];
@@ -2098,7 +2103,7 @@ void ap_sta_set_authorized_event(struct
 					 dpp_pkhash, SHA256_MAC_LEN);
 		}
 
-		hostapd_ubus_notify_authorized(hapd, sta, auth_alg);
+		hostapd_ubus_notify_authorized(hapd, sta, auth_alg, reassoc);
 		wpa_msg(hapd->msg_ctx, MSG_INFO, AP_STA_CONNECTED "%s%s%s%s%s",
 			buf, ip_addr, keyid_buf, dpp_pkhash_buf, alg_buf);
 
@@ -2136,16 +2141,20 @@ void ap_sta_set_authorized_event(struct
 #endif /* CONFIG_FST */
 }
 
-
-bool ap_sta_set_authorized(struct hostapd_data *hapd, struct sta_info *sta,
-			   int authorized)
+bool __ap_sta_set_authorized(struct hostapd_data *hapd, struct sta_info *sta,
+			   int authorized, int reassoc)
 {
 	if (!ap_sta_set_authorized_flag(hapd, sta, authorized))
 		return false;
-	ap_sta_set_authorized_event(hapd, sta, authorized);
+	__ap_sta_set_authorized_event(hapd, sta, authorized, reassoc);
 	return true;
 }
 
+void ap_sta_set_authorized(struct hostapd_data *hapd, struct sta_info *sta,
+			   int authorized)
+{
+	_ap_sta_set_authorized(hapd, sta, authorized, 0);
+}
 
 void ap_sta_disconnect(struct hostapd_data *hapd, struct sta_info *sta,
 		       const u8 *addr, u16 reason)
Index: hostapd-2025.03.10~95ad7115/src/ap/sta_info.h
===================================================================
--- hostapd-2025.03.10~95ad7115.orig/src/ap/sta_info.h
+++ hostapd-2025.03.10~95ad7115/src/ap/sta_info.h
@@ -109,6 +109,7 @@ struct sta_info {
 	struct sta_info_extn sta_extn;
 
 	u8 addr[6];
+	u8 origin_ap[6];
 	be32 ipaddr;
 	struct dl_list ip6addr; /* list head for struct ip6addr */
 	u16 aid; /* STA's unique AID (1 .. 2007) or 0 if not yet assigned */
@@ -480,6 +481,10 @@ void ap_sta_set_authorized_event(struct
 				 struct sta_info *sta, int authorized);
 bool ap_sta_set_authorized(struct hostapd_data *hapd,
 			   struct sta_info *sta, int authorized);
+void _ap_sta_set_authorized(struct hostapd_data *hapd,
+			    struct sta_info *sta, int authorized,
+			    int reassoc);
+
 static inline int ap_sta_is_authorized(struct sta_info *sta)
 {
 	return sta->flags & WLAN_STA_AUTHORIZED;
Index: hostapd-2025.03.10~95ad7115/src/ap/ubus.c
===================================================================
--- hostapd-2025.03.10~95ad7115.orig/src/ap/ubus.c
+++ hostapd-2025.03.10~95ad7115/src/ap/ubus.c
@@ -1769,6 +1769,8 @@ int hostapd_ubus_handle_event(struct hos
 	if (req->ssi_signal)
 		blobmsg_add_u32(&b, "signal", req->ssi_signal);
 	blobmsg_add_u32(&b, "freq", hapd->iface->freq);
+	if (req->reassoc && req->mgmt_frame)
+		blobmsg_add_macaddr(&b, "origin", req->mgmt_frame->u.reassoc_req.current_ap);
 
 	if (req->elems) {
 		if(req->elems->ht_capabilities)
@@ -1815,6 +1817,9 @@ int hostapd_ubus_handle_event(struct hos
 		ubus_notify(ctx, &hapd->ubus.obj, type, b.head, -1);
 		return WLAN_STATUS_SUCCESS;
 	}
+	if (reassoc)
+		blobmsg_add_macaddr(&b, "origin", sta->origin_ap);
+	blobmsg_printf(&b, "target", MACSTR, MAC2STR(hapd->conf->bssid));
 
 	if (ubus_notify_async(ctx, &hapd->ubus.obj, type, b.head, &ureq.nreq))
 		return WLAN_STATUS_SUCCESS;
@@ -1838,12 +1843,15 @@ void hostapd_ubus_notify(struct hostapd_
 
 	blob_buf_init(&b, 0);
 	blobmsg_add_macaddr(&b, "address", addr);
+	
+	blobmsg_add_string(&b, "ifname", hapd->conf->iface);
+	blobmsg_printf(&b, "target", MACSTR, MAC2STR(hapd->conf->bssid));
 
 	ubus_notify(ctx, &hapd->ubus.obj, type, b.head, -1);
 }
 
 void hostapd_ubus_notify_authorized(struct hostapd_data *hapd, struct sta_info *sta,
-				    const char *auth_alg)
+				    const char *auth_alg, int reassoc)
 {
 	if (!hapd->ubus.obj.has_subscribers)
 		return;
@@ -1853,6 +1861,10 @@ void hostapd_ubus_notify_authorized(stru
 	if (auth_alg)
 		blobmsg_add_string(&b, "auth-alg", auth_alg);
 
+	if (reassoc)
+		blobmsg_add_macaddr(&b, "origin", sta->origin_ap);
+	blobmsg_printf(&b, "target", MACSTR, MAC2STR(hapd->conf->bssid));
+
 	ubus_notify(ctx, &hapd->ubus.obj, "sta-authorized", b.head, -1);
 }
 
Index: hostapd-2025.03.10~95ad7115/src/ap/ubus.h
===================================================================
--- hostapd-2025.03.10~95ad7115.orig/src/ap/ubus.h
+++ hostapd-2025.03.10~95ad7115/src/ap/ubus.h
@@ -21,6 +21,7 @@ struct hostapd_ubus_request {
 	const struct ieee802_11_elems *elems;
 	int ssi_signal; /* dBm */
 	const u8 *addr;
+	int reassoc;
 };
 
 struct hostapd_iface;
@@ -67,7 +68,7 @@ int hostapd_ubus_notify_bss_transition_q
 	struct hostapd_data *hapd, const u8 *addr, u8 dialog_token, u8 reason,
 	const u8 *candidate_list, u16 candidate_list_len);
 void hostapd_ubus_notify_authorized(struct hostapd_data *hapd, struct sta_info *sta,
-				    const char *auth_alg);
+				    const char *auth_alg, int reassoc);
 
 #else
 
@@ -146,7 +147,7 @@ static inline int hostapd_ubus_notify_bs
 
 static inline void
 hostapd_ubus_notify_authorized(struct hostapd_data *hapd, struct sta_info *sta,
-			       const char *auth_alg)
+			       const char *auth_alg, int reassoc)
 {
 }
 
