From b48229a18fb92aef272278a71b848ef3816c67f2 Mon Sep 17 00:00:00 2001
From: Thirusenthil Kumaran J <thirusen@qti.qualcomm.com>
Date: Fri, 18 Jul 2025 12:47:27 +0530
Subject: [PATCH 3/4] UPSTREAM: PROTOCOL: hostapd: Add hostapd cli cmd for
 dynamic puncturing

Add support to enable / disable dynamic puncturing using hostapd cli.

Command:
hostapd_cli -i wlanX -l <link_id> set puncture_strict_6ghz <0/1>

0 - Enable dynamic puncturing (No strict puncturing)
1 - Disable dynamic puncturing (Strict puncturing)

Fixes: None
Patch-work: None
Tag: 6G_REGULATORY
Feature: AFC
Patch-dependency:
r82-a-UPSTREAM-PROTOCOL-hostapd-Add-support-for-6-GHz-dyna.patch

Signed-off-by: Thirusenthil Kumaran J <thirusen@qti.qualcomm.com>
---
 hostapd/ctrl_iface.c | 43 +++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 43 insertions(+)

diff --git a/hostapd/ctrl_iface.c b/hostapd/ctrl_iface.c
index 0183fa445c15..125adbb1f6a0 100644
--- a/hostapd/ctrl_iface.c
+++ b/hostapd/ctrl_iface.c
@@ -1185,6 +1185,47 @@ static int hostapd_ctrl_iface_set_band(struct hostapd_data *hapd,
 }
 
 
+/**
+ * hostapd_ctrl_iface_set_punc_strict - Set the puncture strict config
+ * @iface: Pointer to hostapd interface data
+ * @value: Pointer to the string containing the value to set
+ *
+ * Return: 0 on success, -1 on failure
+ */
+static int hostapd_ctrl_iface_set_punc_strict(struct hostapd_iface *iface,
+					      char *value)
+{
+#ifdef NEED_AP_MLME
+	int puncture_strict_config;
+	char *end;
+
+	puncture_strict_config = strtol(value, &end, 10);
+	if (value == end || (puncture_strict_config != 0 &&
+			   puncture_strict_config != 1)) {
+		wpa_printf(MSG_ERROR, "Invalid value for puncture strict config");
+		return -1;
+	}
+
+	if (!is_6ghz_freq(iface->freq)) {
+		wpa_printf(MSG_ERROR, "set_punc_strict is valid only for 6 GHz");
+		return -1;
+	}
+
+	if (iface->conf->puncture_strict_6ghz == puncture_strict_config) {
+		wpa_printf(MSG_DEBUG,
+			   "Puncture strict config already set to %d",
+			   puncture_strict_config);
+		return -1;
+	}
+
+	iface->conf->puncture_strict_6ghz = puncture_strict_config;
+	return 0;
+#else /* NEED_AP_MLME */
+	return -1;
+#endif /* NEED_AP_MLME  */
+}
+
+
 static int hostapd_ctrl_iface_set(struct hostapd_data *hapd, char *cmd)
 {
 	char *value;
@@ -1289,6 +1330,8 @@ static int hostapd_ctrl_iface_set(struct hostapd_data *hapd, char *cmd)
 #endif /* CONFIG_DPP */
 	} else if (os_strcasecmp(cmd, "setband") == 0) {
 		ret = hostapd_ctrl_iface_set_band(hapd, value);
+	} else if (os_strcasecmp(cmd, "puncture_strict_6ghz") == 0) {
+		ret = hostapd_ctrl_iface_set_punc_strict(hapd->iface, value);
 	} else {
 		if (hapd->iface->conf->disable_csa_dfs &&
 		    ((os_strcmp(cmd, "channel") == 0) &&
-- 
2.34.1

