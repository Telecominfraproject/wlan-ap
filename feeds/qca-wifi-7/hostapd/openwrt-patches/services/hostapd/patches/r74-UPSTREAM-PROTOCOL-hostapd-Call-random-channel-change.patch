From 0aa7680de700b22d9660b78071bc28c6576e9ec6 Mon Sep 17 00:00:00 2001
From: Thirusenthil Kumaran J <thirusen@qti.qualcomm.com>
Date: Tue, 10 Jun 2025 21:34:00 +0530
Subject: [PATCH] UPSTREAM:PROTOCOL: hostapd: Call random channel change after
 AFC

After an AFC response is received, the hostapd currently applies
best power mode for the current channel.

For cases, when the best power mode for the current channel is
not SP, then we would operate in LPI power mode, which is
sub-optimal as there could be other channels with higher power.

To enhance this, make a call to random channel selection API,
which will select a random channel from the list of channels
in all supported power modes, and then apply the best power
mode for the selected channel.

The existing logic of applying best power mode for the
current channel is retained as a fallback where random
channel selection fails.

Fixes: None
Patch-work: None
Tag: 6G_REGULATORY
Feature: AFC
Patch-dependency:
n00-001-hostapd-add-support-for-6GHz-operation.patch
r32-001-hostapd-Add-support-to-receive-AFC-power-update-comp.patch
r72-a-UPSTREAM-PROTOCOL-hostapd-6-GHz-power-mode-channels-.patch
r72-d-UPSTREAM-PROTOCOL-hostapd-Clean-up-and-refactor-6-GH.patch
r72-e-UPSTREAM-PROTOCOL-hostapd-Support-for-SP-and-Punctur.patch
r72-f-UPSTREAM-PROTOCOL-hostapd-Add-6-GHz-regulatory-struc.patch
r72-g-UPSTREAM-PROTOCOL-hostapd-Add-6-GHz-base-regulatory-.patch
r73-UPSTREAM-PROTOCOL-hostapd-Select-Random-Channel-Change.patch

Signed-off-by: Thirusenthil Kumaran J <thirusen@qti.qualcomm.com>
---
 src/ap/hostapd.c | 13 +++++++++++++
 1 file changed, 13 insertions(+)

diff --git a/src/ap/hostapd.c b/src/ap/hostapd.c
index 679cd161fa61..b3fd3ad05f2f 100644
--- a/src/ap/hostapd.c
+++ b/src/ap/hostapd.c
@@ -57,6 +57,7 @@
 #include "airtime_policy.h"
 #include "wpa_auth_kay.h"
 #include "hw_features.h"
+#include "interference.h"
 
 
 static int hostapd_flush_old_stations(struct hostapd_data *hapd, u16 reason);
@@ -2354,6 +2355,7 @@ static void channel_list_update_timeout(void *eloop_ctx, void *timeout_ctx)
 static int hostapd_find_random_chan_and_switch(struct hostapd_iface *iface)
 {
 	u8 best_power_mode;
+	int ret;
 
 	if (!is_6ghz_freq(iface->freq)) {
 		wpa_printf(MSG_DEBUG, "Not a 6GHz iface");
@@ -2366,6 +2368,17 @@ static int hostapd_find_random_chan_and_switch(struct hostapd_iface *iface)
 		return -1;
 	}
 
+	/**
+	 * (1) Find a random channel for the current operating BW based on the
+	 *     new AFC payload and regulatory channel lists.
+	 * (2) Calculate the best power mode for the channel.
+	 * (3) Switch to the new channel in the computed power mode.
+	 */
+	ret = hostapd_intf_afc_received(iface);
+	if (!ret)
+		return ret;
+
+	wpa_printf(MSG_ERROR, "Failed to select a random channel");
 	best_power_mode = hostapd_get_best_ap_6ghz_power_mode_for_iface(iface);
 	if (best_power_mode != NL80211_REG_NUM_POWER_MODES) {
 		wpa_printf(MSG_INFO, "%s: Best power mode for Freq %d is %d",
-- 
2.34.1

