From 9340419a632660fcdf446ebbf78bbde9a0acdfb6 Mon Sep 17 00:00:00 2001
From: Harshitha Prem <quic_hprem@quicinc.com>
Date: Sat, 22 Feb 2025 21:51:03 +0530
Subject: [PATCH] hostapd: fix compilation issues

Signed-off-by: Harshitha Prem <quic_hprem@quicinc.com>
---
 hostapd/ctrl_iface.c               |  4 +--
 src/ap/ubus.c                      | 41 ++++++------------------------
 src/ap/ubus.h                      |  2 --
 src/ap/ucode.c                     |  3 +--
 src/ap/wpa_auth.c                  |  4 +--
 src/common/dpp.c                   | 12 ++++-----
 src/common/dpp_crypto.c            |  4 +--
 src/drivers/driver_nl80211_event.c |  5 +++-
 src/radius/radius_das.c            |  6 ++---
 src/radius/radius_das.h            |  4 +--
 src/rsn_supp/wpa.c                 |  2 +-
 src/utils/ucode.c                  | 10 +++-----
 src/wps/httpread.c                 |  4 +--
 wpa_supplicant/ubus.c              |  5 ++--
 wpa_supplicant/ucode.c             | 13 +---------
 15 files changed, 40 insertions(+), 79 deletions(-)

--- a/hostapd/ctrl_iface.c
+++ b/hostapd/ctrl_iface.c
@@ -4724,7 +4724,7 @@ static void hostapd_ctrl_iface_receive(i
 
 	if (os_strcmp(pos, "PING") == 0)
 		level = MSG_EXCESSIVE;
-	wpa_hexdump_ascii(level, "RX ctrl_iface", pos, res);
+	wpa_hexdump_ascii(level, "RX ctrl_iface", (u8 *)pos, res);
 
 	reply_len = hostapd_ctrl_iface_receive_process(hapd, pos,
 						       reply, reply_size,
@@ -4870,7 +4870,7 @@ static void hostapd_mld_ctrl_iface_recei
 	if (os_strcmp(pos, "PING") == 0)
 		level = MSG_EXCESSIVE;
 
-	wpa_hexdump_ascii(level, "RX MLD ctrl_iface", pos, res);
+	wpa_hexdump_ascii(level, "RX MLD ctrl_iface", (u8 *)pos, res);
 
 	reply_len = hostapd_mld_ctrl_iface_receive_process(mld, pos,
 							   reply, reply_size,
--- a/src/ap/ubus.c
+++ b/src/ap/ubus.c
@@ -102,22 +102,6 @@ void hostapd_ubus_free_iface(struct host
 		return;
 }
 
-static void hostapd_notify_ubus(struct ubus_object *obj, char *bssname, char *event)
-{
-	char *event_type;
-
-	if (!ctx || !obj)
-		return;
-
-	if (asprintf(&event_type, "bss.%s", event) < 0)
-		return;
-
-	blob_buf_init(&b, 0);
-	blobmsg_add_string(&b, "name", bssname);
-	ubus_notify(ctx, obj, event_type, b.head, -1);
-	free(event_type);
-}
-
 static void
 hostapd_bss_del_ban(void *eloop_data, void *user_ctx)
 {
@@ -439,9 +423,6 @@ hostapd_notify_response(struct ubus_cont
 {
 	struct blob_attr *tb[__NOTIFY_MAX];
 	struct hostapd_data *hapd = get_hapd_from_object(obj);
-	struct wpabuf *elems;
-	const char *pos;
-	size_t len;
 
 	blobmsg_parse(notify_policy, __NOTIFY_MAX, tb,
 		      blob_data(msg), blob_len(msg));
@@ -580,7 +561,6 @@ hostapd_bss_wps_status(struct ubus_conte
 			struct ubus_request_data *req, const char *method,
 			struct blob_attr *msg)
 {
-	int rc;
 	struct hostapd_data *hapd = container_of(obj, struct hostapd_data, ubus.obj);
 
 	blob_buf_init(&b, 0);
@@ -858,7 +838,6 @@ hostapd_vendor_elements(struct ubus_cont
 static void
 hostapd_rrm_print_nr(struct hostapd_neighbor_entry *nr)
 {
-	const u8 *data;
 	char *str;
 	int len;
 
@@ -906,7 +885,7 @@ __hostapd_bss_mgmt_enable_f(struct hosta
 			WLAN_RRM_CAPS_BEACON_REPORT_ACTIVE |
 			WLAN_RRM_CAPS_BEACON_REPORT_TABLE;
 
-		if (bss->radio_measurements[0] & flags == flags)
+		if (bss->radio_measurements[0])
 			return false;
 
 		bss->radio_measurements[0] |= (u8) flags;
@@ -914,7 +893,7 @@ __hostapd_bss_mgmt_enable_f(struct hosta
 	case BSS_MGMT_EN_LINK_MEASUREMENT:
 		flags = WLAN_RRM_CAPS_LINK_MEASUREMENT;
 
-		if (bss->radio_measurements[0] & flags == flags)
+		if (bss->radio_measurements[0])
 			return false;
 
 		bss->radio_measurements[0] |= (u8) flags;
@@ -927,6 +906,8 @@ __hostapd_bss_mgmt_enable_f(struct hosta
 		bss->bss_transition = 1;
 		return true;
 #endif
+	default:
+		return false;
 	}
 }
 
@@ -965,10 +946,8 @@ hostapd_bss_mgmt_enable(struct ubus_cont
 {
 	struct hostapd_data *hapd = get_hapd_from_object(obj);
 	struct blob_attr *tb[__BSS_MGMT_EN_MAX];
-	struct blob_attr *cur;
 	uint32_t flags = 0;
 	int i;
-	bool neigh = false, beacon = false;
 
 	blobmsg_parse(bss_mgmt_enable_policy, __BSS_MGMT_EN_MAX, tb, blob_data(msg), blob_len(msg));
 
@@ -1177,7 +1156,7 @@ hostapd_rrm_beacon_req(struct ubus_conte
 	struct wpabuf *req;
 	u8 bssid[ETH_ALEN] = { 0xff, 0xff, 0xff, 0xff, 0xff, 0xff };
 	u8 addr[ETH_ALEN];
-	int mode, rem, ret;
+	int ret;
 	int buf_len = 13;
 
 	blobmsg_parse(beacon_req_policy, __BEACON_REQ_MAX, tb, blob_data(msg), blob_len(msg));
@@ -1189,7 +1168,6 @@ hostapd_rrm_beacon_req(struct ubus_conte
 	if (tb[BEACON_REQ_SSID])
 		buf_len += blobmsg_data_len(tb[BEACON_REQ_SSID]) + 2 - 1;
 
-	mode = blobmsg_get_u32(tb[BEACON_REQ_MODE]);
 	if (hwaddr_aton(blobmsg_data(tb[BEACON_REQ_ADDR]), addr))
 		return UBUS_STATUS_INVALID_ARGUMENT;
 
@@ -1465,7 +1443,6 @@ hostapd_bss_transition_request(struct ub
 {
 	struct hostapd_data *hapd = container_of(obj, struct hostapd_data, ubus.obj);
 	struct blob_attr *tb[__BSS_TR_DISASSOC_MAX];
-	struct sta_info *sta;
 	u32 da_timer = 0;
 	u32 valid_period = 0;
 	u8 addr[ETH_ALEN];
@@ -1661,7 +1638,6 @@ void hostapd_ubus_add_bss(struct hostapd
 {
 	struct ubus_object *obj = &hapd->ubus.obj;
 	char *name;
-	int ret;
 
 #ifdef CONFIG_MESH
 	if (hapd->conf->mesh & MESH_ENABLED)
@@ -1679,7 +1655,7 @@ void hostapd_ubus_add_bss(struct hostapd
 	obj->type = &bss_object_type;
 	obj->methods = bss_object_type.methods;
 	obj->n_methods = bss_object_type.n_methods;
-	ret = ubus_add_object(ctx, obj);
+	ubus_add_object(ctx, obj);
 	hostapd_ubus_ref_inc();
 }
 
@@ -1947,7 +1923,6 @@ void hostapd_ubus_notify_bss_transition_
 	const u8 *candidate_list, u16 candidate_list_len)
 {
 #ifdef CONFIG_WNM_AP
-	u16 i;
 
 	if (!hapd->ubus.obj.has_subscribers)
 		return;
@@ -1975,8 +1950,6 @@ int hostapd_ubus_notify_bss_transition_q
 {
 #ifdef CONFIG_WNM_AP
 	struct ubus_event_req ureq = {};
-	char *cl_str;
-	u16 i;
 
 	if (!hapd->ubus.obj.has_subscribers)
 		return 0;
@@ -2002,5 +1975,7 @@ int hostapd_ubus_notify_bss_transition_q
 	ubus_complete_request(ctx, &ureq.nreq.req, 100);
 
 	return ureq.resp;
+#else
+	return 0;
 #endif
 }
--- a/src/ap/ubus.h
+++ b/src/ap/ubus.h
@@ -8,8 +8,6 @@
 #ifndef __HOSTAPD_UBUS_H
 #define __HOSTAPD_UBUS_H
 
-#include "sta_info.h"
-
 enum hostapd_ubus_event_type {
 	HOSTAPD_UBUS_PROBE_REQ,
 	HOSTAPD_UBUS_AUTH_REQ,
--- a/src/ap/ucode.c
+++ b/src/ap/ucode.c
@@ -10,6 +10,7 @@
 #include "dfs.h"
 #include "acs.h"
 #include <libubox/uloop.h>
+#include "sta_info.h"
 
 static uc_resource_type_t *global_type, *bss_type, *iface_type;
 static struct hapd_interfaces *interfaces;
@@ -589,7 +590,6 @@ out:
 
 	for (i = 0; i < iface->num_bss; i++) {
 		struct hostapd_data *hapd = iface->bss[i];
-		int ret;
 
 		hapd->conf->start_disabled = 0;
 		hostapd_set_freq(hapd, conf->hw_mode, iface->freq,
@@ -730,7 +730,6 @@ int hostapd_ucode_init(struct hapd_inter
 		{ "start", uc_hostapd_iface_start },
 		{ "switch_channel", uc_hostapd_iface_switch_channel },
 	};
-	uc_value_t *data, *proto;
 
 	interfaces = ifaces;
 	vm = wpa_ucode_create_vm();
--- a/src/ap/wpa_auth.c
+++ b/src/ap/wpa_auth.c
@@ -4443,7 +4443,7 @@ static u8 * wpa_auth_ml_group_kdes(struc
 	}
 
 	if (!sm->mgmt_frame_prot) {
-		wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %ld",
+		wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %zu",
 			   pos - start);
 		return pos;
 	}
@@ -4486,7 +4486,7 @@ static u8 * wpa_auth_ml_group_kdes(struc
 	}
 
 	if (!sm->wpa_auth->conf.beacon_prot) {
-		wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %ld",
+		wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %zu",
 			   pos - start);
 		return pos;
 	}
@@ -4529,7 +4529,7 @@ static u8 * wpa_auth_ml_group_kdes(struc
 		pos += ml_key_info.links[i].igtk_len;
 	}
 
-	wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %ld", pos - start);
+	wpa_printf(MSG_DEBUG, "RSN: MLO Group KDE len = %zu", pos - start);
 	return pos;
 }
 
@@ -4719,7 +4719,7 @@ static u8 * wpa_auth_ml_kdes(struct wpa_
 	}
 
 	wpa_printf(MSG_DEBUG,
-		   "RSN: MLO Link KDEs and RSN Override Link KDEs len = %ld",
+		   "RSN: MLO Link KDEs and RSN Override Link KDEs len = %zu",
 		   pos - start);
 	pos = wpa_auth_ml_group_kdes(sm, pos, KDE_ALL_LINKS);
 #endif /* CONFIG_IEEE80211BE */
--- a/src/common/dpp.c
+++ b/src/common/dpp.c
@@ -438,7 +438,7 @@ static struct dpp_bootstrap_info * dpp_p
 	const char *version = NULL, *supported_curves = NULL, *host = NULL;
 	struct dpp_bootstrap_info *bi;
 
-	wpa_hexdump_ascii(MSG_DEBUG, "DPP: URI", uri, os_strlen(uri));
+	wpa_hexdump_ascii(MSG_DEBUG, "DPP: URI", (u8 *)uri, os_strlen(uri));
 
 	if (os_strncmp(pos, "DPP:", 4) != 0) {
 		wpa_printf(MSG_INFO, "DPP: Not a DPP URI");
@@ -475,7 +475,7 @@ static struct dpp_bootstrap_info * dpp_p
 		else
 			wpa_hexdump_ascii(MSG_DEBUG,
 					  "DPP: Ignore unrecognized URI parameter",
-					  pos, end - pos);
+					  (u8 *)pos, end - pos);
 		pos = end + 1;
 	}
 
@@ -802,7 +802,7 @@ static struct wpabuf * dpp_build_conf_re
 	}
 	wpa_hexdump(MSG_DEBUG, "DPP: E-nonce", auth->e_nonce, nonce_len);
 	json_len = os_strlen(json);
-	wpa_hexdump_ascii(MSG_DEBUG, "DPP: configRequest JSON", json, json_len);
+	wpa_hexdump_ascii(MSG_DEBUG, "DPP: configRequest JSON", (u8 *)json, json_len);
 
 	/* { E-nonce, configAttrib }ke */
 	clear_len = 4 + nonce_len + 4 + json_len;
@@ -2624,7 +2624,7 @@ static int dpp_parse_cred_legacy(struct
 #endif /* CONFIG_DPP3 */
 
 		wpa_hexdump_ascii_key(MSG_DEBUG, "DPP: Legacy passphrase",
-				      pass->string, len);
+				      (u8 *)pass->string, len);
 		if (dpp_akm_psk(conf->akm) && (len < 8 || len > 63)) {
 			wpa_printf(MSG_DEBUG,
 				   "DPP: Unexpected pass length %zu for a config object that includes PSK",
@@ -3022,7 +3022,7 @@ static int dpp_parse_cred_dpp(struct dpp
 		goto fail;
 	}
 	wpa_hexdump_ascii(MSG_DEBUG, "DPP: signedConnector",
-			  token->string, os_strlen(token->string));
+			  (u8 *)token->string, os_strlen(token->string));
 	signed_connector = token->string;
 
 	if (os_strchr(signed_connector, '"') ||
@@ -3270,7 +3270,7 @@ static int dpp_parse_conf_obj(struct dpp
 			goto fail;
 		}
 		wpa_hexdump_ascii(MSG_DEBUG, "DPP: discovery::ssid",
-				  token->string, os_strlen(token->string));
+				  (u8 *)token->string, os_strlen(token->string));
 		if (os_strlen(token->string) > SSID_MAX_LEN) {
 			dpp_auth_fail(auth,
 				      "Too long discovery::ssid string value");
--- a/src/common/dpp_crypto.c
+++ b/src/common/dpp_crypto.c
@@ -1492,7 +1492,7 @@ dpp_pkex_derive_Qi(const struct dpp_curv
 		len[num_elem] = os_strlen(identifier);
 		num_elem++;
 	}
-	wpa_hexdump_ascii_key(MSG_DEBUG, "DPP: code", code, code_len);
+	wpa_hexdump_ascii_key(MSG_DEBUG, "DPP: code", (u8 *)code, code_len);
 	addr[num_elem] = (const u8 *) code;
 	len[num_elem] = code_len;
 	num_elem++;
@@ -1567,7 +1567,7 @@ dpp_pkex_derive_Qr(const struct dpp_curv
 		len[num_elem] = os_strlen(identifier);
 		num_elem++;
 	}
-	wpa_hexdump_ascii_key(MSG_DEBUG, "DPP: code", code, code_len);
+	wpa_hexdump_ascii_key(MSG_DEBUG, "DPP: code", (u8 *)code, code_len);
 	addr[num_elem] = (const u8 *) code;
 	len[num_elem] = code_len;
 	num_elem++;
--- a/src/radius/radius_das.c
+++ b/src/radius/radius_das.c
@@ -31,7 +31,7 @@ struct radius_das_data {
 	struct dl_list list;
 	struct radius_das_port *port;
 	u8 *shared_secret;
-	u8 *nas_identifier;
+	char *nas_identifier;
 	size_t shared_secret_len;
 	struct hostapd_ip_addr client_addr;
 	unsigned int time_window;
@@ -114,7 +114,7 @@ static struct radius_msg * radius_das_di
 
 	if (radius_msg_get_attr_ptr(msg, RADIUS_ATTR_NAS_IDENTIFIER,
 				    &buf, &len, NULL) == 0) {
-		attrs.nas_identifier = buf;
+		attrs.nas_identifier = (const char *)buf;
 		attrs.nas_identifier_len = len;
 	}
 
@@ -284,7 +284,7 @@ static struct radius_msg * radius_das_co
 
 	if (radius_msg_get_attr_ptr(msg, RADIUS_ATTR_NAS_IDENTIFIER,
 				    &buf, &len, NULL) == 0) {
-		attrs.nas_identifier = buf;
+		attrs.nas_identifier = (const char *)buf;
 		attrs.nas_identifier_len = len;
 	}
 
--- a/src/radius/radius_das.h
+++ b/src/radius/radius_das.h
@@ -22,7 +22,7 @@ enum radius_das_res {
 struct radius_das_attrs {
 	/* NAS identification attributes */
 	const u8 *nas_ip_addr;
-	const u8 *nas_identifier;
+	const char *nas_identifier;
 	size_t nas_identifier_len;
 	const u8 *nas_ipv6_addr;
 
@@ -44,7 +44,7 @@ struct radius_das_attrs {
 struct radius_das_conf {
 	int port;
 	const u8 *shared_secret;
-	const u8 *nas_identifier;
+	const char *nas_identifier;
 	size_t shared_secret_len;
 	const struct hostapd_ip_addr *client_addr;
 	unsigned int time_window;
--- a/src/rsn_supp/wpa.c
+++ b/src/rsn_supp/wpa.c
@@ -777,7 +777,7 @@ static int wpa_handle_ext_key_id(struct
 
 		if (!kde->key_id) {
 			wpa_msg(sm->ctx->msg_ctx,
-				sm->use_ext_key_id ? MSG_INFO : MSG_DEBUG,
+				MSG_INFO,
 				"RSN: No Key ID in Extended Key ID handshake");
 			sm->keyidx_active = 0;
 			return sm->use_ext_key_id ? -1 : 0;
--- a/src/utils/ucode.c
+++ b/src/utils/ucode.c
@@ -78,13 +78,13 @@ static void uc_gc_timer(struct uloop_tim
 uc_value_t *uc_wpa_printf(uc_vm_t *vm, size_t nargs)
 {
 	uc_value_t *level = uc_fn_arg(0);
-	uc_value_t *ret, **args;
+	uc_value_t *ret;
 	uc_cfn_ptr_t _sprintf;
 	int l = MSG_INFO;
-	int i, start = 0;
+	int start = 0;
 
 	_sprintf = uc_stdlib_function("sprintf");
-	if (!sprintf)
+	if (!_sprintf)
 		return NULL;
 
 	if (ucv_type(level) == UC_INTEGER) {
@@ -195,7 +195,6 @@ uc_value_t *uc_wpa_freq_info(uc_vm_t *vm
 	center_idx = (center_idx - channel) * 5 + freq_val;
 	ucv_object_add(ret, "center_freq1", ucv_int64_new(center_idx));
 
-out:
 	return ret;
 }
 
@@ -223,7 +222,7 @@ uc_value_t *uc_wpa_sha1(uc_vm_t *vm, siz
 		if (ucv_type(val) != UC_STRING)
 			return NULL;
 
-		args[i] = ucv_string_get(val);
+		args[i] = (const u8 *)ucv_string_get(val);
 		lens[i] = ucv_string_length(val);
 	}
 
@@ -443,7 +442,6 @@ uc_value_t *wpa_ucode_global_init(const
 
 int wpa_ucode_registry_add(uc_value_t *reg, uc_value_t *val)
 {
-	uc_value_t *data;
 	int i = 0;
 
 	while (ucv_array_get(reg, i))
--- a/src/wps/httpread.c
+++ b/src/wps/httpread.c
@@ -382,7 +382,7 @@ static void httpread_read_handler(int sd
 		wpa_printf(MSG_DEBUG, "httpread failed: %s", strerror(errno));
 		goto bad;
 	}
-	wpa_hexdump_ascii(MSG_MSGDUMP, "httpread - read", readbuf, nread);
+	wpa_hexdump_ascii(MSG_MSGDUMP, "httpread - read", (u8 *)readbuf, nread);
 	if (nread == 0) {
 		/* end of transmission... this may be normal
 		 * or may be an error... in some cases we can't
@@ -695,7 +695,7 @@ got_file:
 	wpa_printf(MSG_DEBUG, "httpread got file %d bytes type %d",
 		   h->body_nbytes, h->hdr_type);
 	wpa_hexdump_ascii(MSG_MSGDUMP, "httpread: body",
-			  h->body, h->body_nbytes);
+			  (u8 *)h->body, h->body_nbytes);
 	/* Null terminate for convenience of some applications */
 	if (h->body)
 		h->body[h->body_nbytes] = 0; /* null terminate */
--- a/wpa_supplicant/ubus.c
+++ b/wpa_supplicant/ubus.c
@@ -126,7 +126,7 @@ wpas_bss_wps_start(struct ubus_context *
 {
 	int rc;
 	struct wpa_supplicant *wpa_s = get_wpas_from_object(obj);
-	struct blob_attr *tb[__WPS_START_MAX], *cur;
+	struct blob_attr *tb[__WPS_START_MAX];
 	int multi_ap = 0;
 
 	blobmsg_parse(wps_start_policy, __WPS_START_MAX, tb, blobmsg_data(msg), blobmsg_data_len(msg));
@@ -175,7 +175,6 @@ void wpas_ubus_add_bss(struct wpa_suppli
 {
 	struct ubus_object *obj = &wpa_s->ubus.obj;
 	char *name;
-	int ret;
 
 	if (!wpas_ubus_init())
 		return;
@@ -187,7 +186,7 @@ void wpas_ubus_add_bss(struct wpa_suppli
 	obj->type = &bss_object_type;
 	obj->methods = bss_object_type.methods;
 	obj->n_methods = bss_object_type.n_methods;
-	ret = ubus_add_object(ctx, obj);
+	ubus_add_object(ctx, obj);
 	wpas_ubus_ref_inc();
 }
 
--- a/wpa_supplicant/ucode.c
+++ b/wpa_supplicant/ucode.c
@@ -7,6 +7,7 @@
 #include "wps_supplicant.h"
 #include "bss.h"
 #include "ucode.h"
+#include "driver_i.h"
 
 static struct wpa_global *wpa_global;
 static uc_resource_type_t *global_type, *iface_type;
@@ -32,7 +33,6 @@ wpas_ucode_update_interfaces(void)
 {
 	uc_value_t *ifs = ucv_object_new(vm);
 	struct wpa_supplicant *wpa_s;
-	int i;
 
 	for (wpa_s = wpa_global->ifaces; wpa_s; wpa_s = wpa_s->next)
 		ucv_object_add(ifs, wpa_s->ifname, ucv_get(wpas_ucode_iface_get_uval(wpa_s)));
@@ -43,8 +43,6 @@ wpas_ucode_update_interfaces(void)
 
 void wpas_ucode_add_bss(struct wpa_supplicant *wpa_s)
 {
-	uc_value_t *val;
-
 	if (wpa_ucode_call_prepare("iface_add"))
 		return;
 
@@ -94,7 +92,6 @@ void wpas_ucode_update_state(struct wpa_
 
 void wpas_ucode_event(struct wpa_supplicant *wpa_s, int event, union wpa_event_data *data)
 {
-	const char *state;
 	uc_value_t *val;
 
 	if (event != EVENT_CH_SWITCH_STARTED)
@@ -125,13 +122,6 @@ void wpas_ucode_event(struct wpa_supplic
 	ucv_gc(vm);
 }
 
-static const char *obj_stringval(uc_value_t *obj, const char *name)
-{
-	uc_value_t *val = ucv_object_get(obj, name, NULL);
-
-	return ucv_string_get(val);
-}
-
 static uc_value_t *
 uc_wpas_add_iface(uc_vm_t *vm, size_t nargs)
 {
@@ -267,7 +257,6 @@ int wpas_ucode_init(struct wpa_global *g
 	static const uc_function_list_t iface_fns[] = {
 		{ "status", uc_wpas_iface_status },
 	};
-	uc_value_t *data, *proto;
 
 	wpa_global = gl;
 	vm = wpa_ucode_create_vm();
--- a/src/common/sae.c
+++ b/src/common/sae.c
@@ -2113,7 +2113,7 @@ static int sae_parse_password_identifier
 	os_memcpy(sae->tmp->parsed_pw_id, epos, len);
 	sae->tmp->parsed_pw_id[len] = '\0';
 	wpa_hexdump_ascii(MSG_DEBUG, "SAE: Received Password Identifier",
-			  sae->tmp->parsed_pw_id, len);
+			  (u8 *)sae->tmp->parsed_pw_id, len);
 	*pos = epos + len;
 	return WLAN_STATUS_SUCCESS;
 }
--- a/src/drivers/driver_nl80211_event.c
+++ b/src/drivers/driver_nl80211_event.c
@@ -189,6 +189,8 @@ static const char * nl80211_command_to_s
 	C2S(NL80211_CMD_SET_TID_TO_LINK_MAPPING)
 	C2S(NL80211_CMD_ASSOC_MLO_RECONF)
 	C2S(NL80211_CMD_EPCS_CFG)
+	C2S(NL80211_CMD_STOP_BGRADAR_DETECT)
+	C2S(NL80211_CMD_INTERFERENCE_DETECT)
 	C2S(__NL80211_CMD_AFTER_LAST)
 	}
 #undef C2S
--- a/src/ap/ieee802_11_eht.c
+++ b/src/ap/ieee802_11_eht.c
@@ -1222,7 +1222,7 @@ u16 hostapd_process_ml_assoc_req(struct
 		wpa_printf(MSG_DEBUG, "MLD: EXT ML capabilities not present");
 	}
 
-	wpa_printf(MSG_DEBUG, "MLD: expected_common_info_len=%lu",
+	wpa_printf(MSG_DEBUG, "MLD: expected_common_info_len=%zu",
 		   common_info_len);
 
 	if (sizeof(*ml) + common_info_len > ml_len) {
