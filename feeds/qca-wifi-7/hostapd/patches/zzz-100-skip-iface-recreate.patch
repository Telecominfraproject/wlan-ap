--- a/src/drivers/driver_nl80211.c
+++ b/src/drivers/driver_nl80211.c
@@ -14494,6 +14494,8 @@ static int wpa_driver_nl80211_can_share_
 	const u8 *addr;
 	int ret = -1;
 
+	return -1;
+
 	/* create a dummy drv to read the phyname */
 	if (params->global_priv == NULL)
 		return ret;
